# worker.js imports ./screener_{engine,data,score}.js and ./valuation_{data,model}.js
# — wrangler bundles them via esbuild. Deploys are triggered by any change under
# cloudflare/, so a new module here ships without touching this file.
name            = "yf-proxy"
main            = "worker.js"
compatibility_date = "2025-01-01"
account_id      = "5b850153715d741a6c323976c2d0657a"
